<div id="gameEditModal" class="modal fade bs-example-modal-sm" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
      <div id='store'>
      <%= form_tag(search_path, method: :get) do %>
        <%= text_field_tag(:q, '', placeholder: 'Type the name of your favorite game', class: 'form-control', autocomplete: "off", autofocus: true, id: "live-search") %>
        <div id="live-search-results">
          <!-- convert newVal str to array, for each value, find game id and display -->
        </div>
      <%# (@listgame.length).times do |list| %>
      <%# game = Game.find(@listgame[list][0]) %>
      <%# end %>
      <% end %>
      </div>
    </div>
  </div>
</div>
