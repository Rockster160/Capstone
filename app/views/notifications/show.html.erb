<div class='container'>
  <h1>Notifications</h1>
  <hr/>
  <% @user.notifications.reverse.first(50).each do |notify| %>
    <div class='well'>
      <%= link_to image_tag(Game.find(notify.game_id).ava, class: 'notification-image'), game_path(notify.game_id) %>
      <%= notify.title %>
      <%= notify.message %>
      <br/>
    </div>
  <% end %>
</div>
